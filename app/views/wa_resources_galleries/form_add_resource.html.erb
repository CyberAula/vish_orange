<div class="modal-header">
  <button type="button" class="close" data-dismiss="modal" aria-hidden="true">x</button>
  <h1><%= t('workshop.activities.resource_gallery.add') %></h1>
</div>

<%= form_for resource, :remote => false do |f| %>
  <div class="modal-body tabbable tabs-below">
      <%= f.hidden_field :workshop_id, :value => resource.workshop.id %>
      <p class="description"><%= t('workshop.activities.resource.modal_instructions') %></p>
      <input type="text" class="form-control" name="url"/>
      <%= f.submit t('save'), :class => "hidden"  %>
  </div>
  <div class="modal-footer">
    <%= f.submit t('save'), :class => "btn btn-primary" %>
  </div>
<% end %>
